package com.analyticobjects.digitalsafe.containers;

/**
 * A note type to store arbitrary files.
 * @author Joel Bondurant
 * @since 2013.09
 */
public class FileNote extends Note {

    public FileNote(String title, String message) {
        super(title, message);
    }
    
}
